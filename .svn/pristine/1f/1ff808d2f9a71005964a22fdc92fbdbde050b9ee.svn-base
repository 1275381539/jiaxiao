  <view class="page-section page-section-gap">
    <!-- <map
      id="myMap"
      style="width: 100%; height: 300px;"
      latitude="{{latitude}}"
      longitude="{{longitude}}"
      markers="{{markers}}"
      covers="{{covers}}"
      show-location="true"
    ></map> -->
      <map
      id="myMap"
      style="width: 100%; height:1000rpx;margin-top:10rpx;"
      latitude="{{school.school_location_x}}"
      longitude="{{school.school_location_y}}"
      scale='{{scale}}'
      markers="{{markers}}"
      show-location="true"
    ></map>
  </view>
